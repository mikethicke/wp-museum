{
  "name": "admin-react",
  "version": "0.0.6",
  "description": "React in WP Dashboard",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wp-scripts build /src/index.js --output-path=build",
    "start": "wp-scripts start /src/index.js --output-path=build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/components": "^9.8.0",
    "@wordpress/scripts": "^11.0.0"
  }
}
